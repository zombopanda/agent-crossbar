# Agent Crossbar v0.3.4

Reasonix print jobs now stream their live provider output through `job_tail`.

## Added

- `job_tail` exposes full and incremental stdout for Reasonix print jobs while
  they are still running, using the same cursor semantics as other transports.

## Fixed

- Claude lifecycle contract tests no longer require a locally installed,
  authenticated Claude CLI or live model discovery.
